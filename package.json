{
  "name": "backend-project",
  "version": "1.0.0",
  "main": "index.js",
  "type":"module",
  "scripts": {
    "dev":"node --watch --env-file=.env --experimental-sqlite --experimental-strip-types ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": " A full stack To-do application solemnly on node.js using sqlite and jwt authentication.",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2"
  }
}
